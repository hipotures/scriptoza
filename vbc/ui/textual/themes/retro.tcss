/* Terminal Retro Theme - Phosphor green on black, vintage terminal style */

$bg: #000000;
$bg-surface: #0a0a0a;
$bg-panel: #0f0f0f;
$primary: #33ff33;
$secondary: #22cc22;
$accent: #44ff44;
$warning: #aaaa00;
$error: #ff3333;
$success: #33ff33;
$text: #33ff33;
$text-dim: #1a7a1a;
$border: #1a7a1a;

Screen {
    background: $bg;
    color: $text;
}

#main-container {
    background: $bg;
}

/* Header */
#header {
    dock: top;
    height: 3;
    background: $bg-surface;
    border-bottom: ascii $border;
    padding: 0 1;
}

#header .status-active {
    color: $primary;
    text-style: bold;
}

#header .status-shutdown {
    color: $warning;
    text-style: bold;
}

#header .status-interrupt {
    color: $error;
    text-style: bold;
}

/* Progress panel */
#progress-panel {
    height: 4;
    background: $bg-panel;
    border: ascii $border;
    margin: 0 1;
    padding: 0 1;
}

#progress-panel ProgressBar > .bar--bar {
    color: $primary;
    background: $bg-surface;
}

#progress-panel ProgressBar > .bar--complete {
    color: $accent;
}

/* GPU Sparkline */
#gpu-sparkline {
    height: 4;
    background: $bg-panel;
    border: ascii $border;
    margin: 0 1;
    padding: 0 1;
}

.sparkline-cool {
    color: $primary;
}

.sparkline-warm {
    color: $warning;
}

.sparkline-hot {
    color: $error;
}

/* Active jobs */
#active-jobs {
    background: $bg-panel;
    border: ascii $border;
    margin: 0 1;
    padding: 0 1;
}

#active-jobs .job-row {
    height: 3;
    padding: 0 1;
}

#active-jobs .job-row:hover {
    background: $bg-surface;
}

#active-jobs .job-row:focus {
    background: $text-dim;
}

.spinner {
    color: $primary;
}

.job-filename {
    color: $text;
}

.job-metadata {
    color: $text-dim;
}

.job-progress {
    color: $primary;
}

/* Activity feed */
#activity-feed {
    background: $bg-panel;
    border: ascii $border;
    margin: 0 1;
    padding: 0 1;
}

.status-success {
    color: $success;
}

.status-error {
    color: $error;
}

.status-kept {
    color: $text-dim;
}

.status-interrupted {
    color: $error;
}

/* Queue panel */
#queue-panel {
    background: $bg-panel;
    border: ascii $border;
    margin: 0 1;
    padding: 0 1;
}

#queue-panel DataTable {
    background: transparent;
}

#queue-panel DataTable > .datatable--header {
    background: $bg-surface;
    color: $primary;
}

#queue-panel DataTable > .datatable--cursor {
    background: $text-dim;
}

.queue-indicator {
    color: $text-dim;
}

/* Health footer */
#health-footer {
    dock: bottom;
    height: 1;
    background: $bg-surface;
    border-top: ascii $border;
    padding: 0 1;
}

.health-fail {
    color: $error;
}

.health-warning {
    color: $warning;
}

.health-dim {
    color: $text-dim;
}

/* Overlays */
ModalScreen {
    align: center middle;
}

#overlay-container {
    background: $bg-panel;
    border: ascii $primary;
    padding: 1 2;
    width: 60;
    max-height: 80%;
}

#overlay-container .overlay-title {
    color: $primary;
    text-align: center;
    padding-bottom: 1;
    border-bottom: ascii $border;
}

/* Job details */
#job-details {
    background: $bg-panel;
    border: ascii $primary;
    padding: 1 2;
    width: 70;
    height: auto;
    max-height: 90%;
}

#job-details .detail-label {
    color: $text-dim;
    width: 15;
}

#job-details .detail-value {
    color: $text;
}

#job-details .ffmpeg-command {
    background: $bg;
    color: $primary;
    padding: 1;
    margin-top: 1;
}

/* Stats browser */
#stats-browser {
    background: $bg;
}

#stats-browser #categories {
    width: 20;
    background: $bg-panel;
    border-right: ascii $border;
}

#stats-browser #categories ListView > ListItem {
    padding: 0 1;
}

#stats-browser #categories ListView > ListItem:hover {
    background: $bg-surface;
}

#stats-browser #categories ListView > ListItem.-active {
    background: $text-dim;
    color: $bg;
}

#stats-browser #files-table {
    background: $bg-panel;
}

/* Buttons */
Button {
    background: $bg-surface;
    color: $primary;
    border: ascii $border;
    margin: 0 1;
}

Button:hover {
    background: $text-dim;
}

Button:focus {
    border: ascii $primary;
}

Button.-primary {
    color: $bg;
    background: $primary;
}

/* Panel titles */
.panel-title {
    color: $primary;
    padding-bottom: 1;
}

/* Scrollbars */
Scrollbar {
    background: $bg-surface;
}

ScrollbarSlider {
    background: $text-dim;
}

ScrollbarSlider:hover {
    background: $primary;
}

/* Footer */
Footer {
    background: $bg-surface;
}

Footer > .footer--key {
    background: $text-dim;
    color: $bg;
}

Footer > .footer--description {
    color: $text-dim;
}
