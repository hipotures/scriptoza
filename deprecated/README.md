# Deprecated Tools

Legacy scripts kept for reference and backward compatibility.

- `video/rename_video.py` - Deprecated legacy video renamer kept outside the active toolset and not installed by `utils/install.py`
- `pipeline/` - Deprecated event workflow snapshot kept only as a compatibility copy after migration to `vocatio`
